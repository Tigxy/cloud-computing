apiVersion: v1
kind: Pod
metadata:
  name: discord-service
spec:
  containers:
    - name: discord-service
      image: ticks/discord-bot:0.0.1
      env:
        - name: DISCORD_TOKEN
          value: "<SECRET_DISCORD_TOKEN>" 
      # ports:
      #   - name: web
      #     containerPort: 8888
      #     protocol: TCP